---
title: "Övning med databasen World och SELECT" 
description: "Övning för att träna på hur en databas ser ut och hur man kan skapa rapporter från den med SQL."
author: mos
revision:
    "2026-01-09": "(A, mos) Första versionen."
sidebar:
    order: 0010
---

import Figure from '@components/CustomFigure.astro';
import YouTube from '@components/YouTube.astro';

{/*
TODO

* 
* 

*/}

:::danger
Denna övningen är inte påbörjad/klar.
:::

I denna övningen skall vi använda en befintlig databas som heter World. Databasen innehåller information om världens länder, städer och kontinenter. Vi skall läsa in databasen i vår MariaDB-server och sedan använda både terminalklienten och desktopklienten för att skapa SQL-frågor mot databasen.



## Förutsättningar

Du har installerat labbmiljön för kursen och du har tillgång till terminalklienten och desktopklienten.

Du är van att navigera i terminalen.

Du kan grunderna i SQL.



## Spara ditt arbete i ditt kursrepo

I ditt kursrepo, skapa följande katalog där du kan spara ditt arbete med denna övning:

```bash title="Skapa katalog för övningen"
# Gå till ditt kursrepo
mkdir -p kmom/01/world1
cd kmom/01/world1

# Öppna katalogen i VS Code
code .
```

Skapa filen `select.sql` och spara all SQL-kod du skriver på egen hand under övningen i den filen.

Börja med att lägga till följande header i filen `select.sql`.

```sql title="select.sql"
-- SQL-kod för övningen Databasen World
-- Kurs: Databasteknik
-- Författare: Ditt Namn
-- Datum: ÅÅÅÅ-MM-DD
```

Uppdatera med rätt datum och namn. Du kan skriva kommentarerna på engelska om du vill.



## Ladda in databasen world från backupfilen

I ditt kursrepo ligger en backupfil för databasen World som du kan använda för att läsa in databasen i din MariaDB-server. Du hittar backupfilen i `example/db/world.sql`.

Du kan läsa in den så här i terminalklienten.

```bash title="Läs in databasen world i terminalklienten"
# Om du står i roten av ditt kursrepo
mariadb < example/db/world.sql

# Om du står i katalogen kmom/01/world1
mariadb < ../../../example/db/world.sql
```

Om du inte ser nån utskrift så har det (troligen) gått bra.

Du kan du verifiera att databasen finns genom att lista databaserna i terminalklienten.

```sql title="Verifiera att databasen world finns"
mariadb world -e "SHOW TABLES"
```

Utskriften kan se ut så här.

```text title="Exempelutskrift från SHOW TABLES"
$ mariadb world -e "SHOW TABLES"
+-----------------+
| Tables_in_world |
+-----------------+
| city            |
| country         |
| countrylanguage |
+-----------------+
```

Nu kan du öppna terminalklienten mot databasen world.

```bash title="Öppna terminalklienten mot databasen world"
mariadb
```

När du kommit in i klienten kan du byta till databasen world och dubbelkolla vilka tabeller som finns i databasen igen.

```sql title="Byt till databasen world"
USE world;

SHOW TABLES;
```

Det kan se ut så här när du kör kommandona.

```text title="Exempelutskrift från USE och SHOW TABLES"
$ mariadb

MariaDB [(none)]> USE world;
Database changed

MariaDB [world]> SHOW TABLES;
+-----------------+
| Tables_in_world |
+-----------------+
| city            |
| country         |
| countrylanguage |
+-----------------+
3 rows in set (0,000 sec)
```

Nu är du redo att börja jobba med övningen.



## Övningen

Övningen ligger i en artikel i ett repo på GitLab och är beskriven i en README.md fil. Du kan hitta den SQL-kod som används i övningen på samma plats.

Genomför nu övningen som du hittar här:

* [World database and SQL: Get going with SELECT](https://gitlab.com/mikael-roos/database/-/tree/main/sql/world/get-going-v2)

Glöm inte att spara alla SQL-kod du skriver i filen `select.sql` i ditt kursrepo.

När du är klar med övningen så finns det en uppgift som bygger på övningen.
