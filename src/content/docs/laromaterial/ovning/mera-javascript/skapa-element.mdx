---
title: "Skapa element" 
sidebar:
    order: 0030
---

import Figure from '@components/CustomFigure.astro';
import createElement from '@assets/kmom04/createelement.png';

Vi kan även skapa html-element i JavaScript. Det görs med den inbyggda metoden `document.createElement()`. Låt säga att vi vill skapa 5 lådor, där vi har stylat klassnamnet `box` i css. 

Om du vill koda med kan du hämta [sandboxen](https://github.com/webtec-2024/teacher/tree/main/example/sandbox/sandbox.html).

```css title="css"
.box {
  width: 100px;
  height: 100px;
  border: 1px solid black;
  display: inline-block;
  margin: 2px;
}
```

Dessa lådor vill vi placera inuti vår "content div".

Först html koden:

```html title="html"
<div id="content" class="content">
</div>
```

Sedan skapar vi element på följande sätt:

```js title="js, createElement(), appendChild()"
const myContent = document.getElementsByClassName('content')[0]

for (let i = 0; i < 5; i++) {
  const box = document.createElement("div")

  box.className = "box"

  myContent.appendChild(box)
}
```

Vi använder den inbyggda funktionen `element.appendChild` för att lägga till box-elementen inuti content-elementet. Det går att skapa väldigt många html-element på liknande sätt och manipulera dem i JavaScript.



<Figure 
    src={createElement}
    caption="Slutresultat, 5 lådor skapade och utritade."
/>

Läs mer om [createElement](https://developer.mozilla.org/en-US/docs/Web/API/Document/createElement).  
Läs mer om [appendChild](https://developer.mozilla.org/en-US/docs/Web/API/Node/appendChild).